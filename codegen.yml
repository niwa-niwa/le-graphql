overwrite: true
generates:
  ./src/types/generated/graphql.ts:
    schema: schema.graphql
    config:
      useIndexSignature: true
      contextType: ../context
    plugins:
      - typescript
      - typescript-resolvers
      